# Work Profile — deploy on your work laptop
PROFILE_NAME=Work
PROFILE_COLOR=#3b82f6
INTEGRATIONS=ado

# Data persistence
APP_DATA_PATH=C:\Users\cacarlt\docker\appdata\flowstate

# ADO credentials
ADO_ORG=IdentityDivision
ADO_PROJECT=Engineering
ADO_PAT=your-personal-access-token

# PAT lifecycle (filled by deploy.sh)
# ADO_PAT_AUTH_ID=
# ADO_PAT_NAME=
# ADO_PAT_ORG=IdentityDivision

# Server
PORT=3001
